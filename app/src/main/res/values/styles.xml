<resources>

    <style name="Play_SeekBar" parent="@android:style/Widget.SeekBar">
        <item name="android:maxHeight">1.0dip</item>
        <item name="android:minHeight">1.0dip</item>
        <item name="android:height">1.0dip</item>
        <item name="android:progressDrawable">@drawable/play_seekbar_background</item>
        <item name="android:thumb">@drawable/play_seekbar_thumb</item>
        <item name="android:thumbOffset">0dp</item>
        <item name="android:indeterminateOnly">false</item>

    </style>

    <!--自定义dialog背景全透明无边框theme -->
    <style name="MyDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- Base application theme. -->
    <style name="CustomDialog" parent="android:style/Theme.Dialog">

        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@color/translucent_black</item>
        <!-- 是否有标题 -->
        <!-- <item name="android:windowNoTitle">true</item> -->
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 不显示边框 -->
        <!-- <item name="android:windowFrame">@null</item> -->
    </style>

    <style name="Widget.GifView" parent="@android:style/Widget"></style>

    <!-- fecth_image_popupwindow -->
    <style name="fetch_image_popup_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popup_enter</item>
        <item name="android:windowExitAnimation">@anim/popup_exit</item>
    </style>
    <!-- 去掉背景Dialog -->
    <style name="NobackDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 分享对话框样式 -->
    <style name="ShareDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
